import { EditProfButton } from "./EditProfileButton.style";

function UpdateImageButton(props) {
  //open dialog to upload an image
  const handleEditClick = () => {};

  return <EditProfButton onClick={handleEditClick}>UPDATE IMAGE</EditProfButton>;
}

export default UpdateImageButton;
